import { Box } from "@mui/material";
import { ReactNode } from "react";

const AuthCardContent = ({ children }: { children: ReactNode }) => (
  <Box>{children}</Box>
);

export default AuthCardContent;
